import React from 'react';
import {
    HeroContainer,
    HeroContent
} from './heroElements';


const Hero = () => {
    return (
        <HeroContainer>
            <HeroContent>Hero here</HeroContent>
        </HeroContainer>
    );
}

export default Hero;